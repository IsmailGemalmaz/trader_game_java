<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:textColorPrimary">@color/primary_dark_text</item>
        <item name="android:textColorSecondary">@color/secondary_dark_text</item>
        <item name="android:textColor">@color/primary_dark_text</item>
    </style>

    <!-- Varsayılan activity teması -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- GetİnforMation Teması-->
    <style name="AppTheme.NoActionBar.GetInformation">
        <item name="colorPrimaryDark">@color/primary</item>
    </style>

    <!-- Arkaplanı transparan activity teması -->
    <style name="AppTheme.NoActionBar.Translucent">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/primary_text_light</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Custom dialog tasarlandığında butonlarına bu stil set'lenir -->
    <style name="DialogButton" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">54dp</item>
        <item name="android:background">?selectableItemBackground</item>
        <item name="android:textSize">15dp</item>

    </style>
</resources>